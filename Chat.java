class Chat extends JFrame {

    private connections_info;

    public Chat(String connections_info, String title) {
        super("Chat " + title);
        this.connections_info = connections_info;
    }

}